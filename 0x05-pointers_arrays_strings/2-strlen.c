#include "main.h"
#include <string.h>
/**
 * _strlen - returns the length of a string
 * @s: string to check for length
 *
 * Return: Always 0.
 */

int _strlen(char *s)
{
	int n;

	n = strlen(s);

	return (n);
}
